{{- if .Values.clusterAutoscaler.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.namePrefix | default "" }}{{ .Values.clusterAutoscaler.name }}{{ .Values.nameSuffix | default "" }}
  namespace: {{ .Values.clusterAutoscaler.namespace }}
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ .Values.clusterAutoscaler.project }}
  destination:
    server: {{ .Values.clusterAutoscaler.destination.server }}
    namespace: {{ .Values.clusterAutoscaler.destination.namespace }}
  source:
    path: {{ .Values.clusterAutoscaler.source.path }}
    repoURL: {{ .Values.clusterAutoscaler.source.repoURL }}
    targetRevision: {{ .Values.clusterAutoscaler.source.targetRevision }}
    chart: {{ .Values.clusterAutoscaler.source.chart }}
    {{- if and .Values.clusterAutoscaler.source.helm (not (empty .Values.clusterAutoscaler.source.helm.valuesObject)) }}
    helm:
      valuesObject: {{ toYaml .Values.clusterAutoscaler.source.helm.valuesObject | nindent 8 }}
    {{- end }}
  {{- if and .Values.clusterAutoscaler.syncPolicy (not (empty .Values.clusterAutoscaler.syncPolicy)) }}
  syncPolicy:
{{ toYaml .Values.clusterAutoscaler.syncPolicy | indent 4 }}
  {{- end }}
{{- end }}
